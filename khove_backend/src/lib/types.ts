/**
 * lib/types.ts
 *
 * Re-export barrel — all types live in lib/types/*.
 * This file exists for backward compatibility so all existing
 * `import ... from "@backend/lib/types"` paths continue to work.
 */
export * from "./types/index";
